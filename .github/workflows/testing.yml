name: Python Database Tests

on:
  push:
    branches:
      - "main"


jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v3

      - name: Check if python in the environment
        run: docker build -t docker-python-interfaces -f docker/Dockerfile.dev .

      - name: Run tests 🧪
        run: docker run docker-python-interfaces pytest -svv