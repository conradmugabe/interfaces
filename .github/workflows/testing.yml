name: Python Database Tests

on:
  push:
    branches:
      - "main"


jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v3

      - name: Check if python in the environment
        uses: python3 --version

      - name: Run tests 🧪
        run: python3 --version