name: Python Database Tests

on:
  push:
    branches:
      - "main"


jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v3

      - name: List All files and folders in the current directory ⚙️
        run: ls -a